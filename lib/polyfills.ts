// Crypto polyfill — MUST be imported before any @solana/web3.js usage
// react-native-get-random-values provides crypto.getRandomValues()
// which is required by Solana's cryptographic operations.

import 'react-native-get-random-values';
